<% if @questions.empty? %>
  $("#routine-questions").empty();
  $('#editor-info').show();
<% else %>
  var new_render= $('<%= j render @questions %>');
  $("#routine-questions").html(new_render);
<% end %>

